const express = require("express");
const router = express.Router();
const { db } = require("../firebase/config");
const trackNames = require("../firebase/trackNames")

router.post("/", async (req, res) => {
    try {

    } catch (error) {
    
    }
})

module.exports = router;